qsort([]) = [].
qsort([H|T]) = L =>
    L = qsort([E : E in T, E =< H]) ++
        [H] ++
        qsort([E : E in T, E > H]).

main =>
    L = [17, 3, 41, 25, 8, 1, 6, 40],
    println("Original list:"),
    println(L),

    Sorted = qsort(L),
    println("Sorted list:"),
    println(Sorted).
